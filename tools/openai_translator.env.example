# OpenAI-compatible translator settings (example)
# Copy this file to tools/openai_translator.env and fill your secrets.

# Base URL to the OpenAI-compatible API
# e.g. https://api.openai.com or your self-hosted endpoint
OPENAI_BASE_URL=

# API key (Bearer). Leave empty if your server doesn't require it.
OPENAI_API_KEY=

# Default model name, e.g. gpt-4o-mini, gpt-4o, gpt-4.1, llama3.1:latest
OPENAI_MODEL=
